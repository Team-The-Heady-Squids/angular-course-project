import { ZoomInTextDirective } from './zoom-in-text.directive';

describe('ZoomInTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomInTextDirective();
    expect(directive).toBeTruthy();
  });
});
